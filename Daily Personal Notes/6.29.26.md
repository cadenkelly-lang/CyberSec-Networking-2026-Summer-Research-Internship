# Notes/Done
- ~~Setting up the firewall config from Friday on the other 12 VLANs through their OPNsense firewall
- **Configured the 16 port switch as much as psosible rn
- ~~USB Backups
	- Should run all the time
	- Updates at 3pm every day
	- Keep plugged

# Notes
- won't need sfp ports
- Want to scrap diagrams and make another using the .9.xx network for the glinet outbound subnet. 
- How to manage .10.xx?
	- GL Inet is the router firewall, dhcp, dns for .10 - all switches, proxmox, jump host, opnsense. all opnsense are getting dhcp from it so we must statically assign
- How students going to connect to PTG
	- could let dhcp vm give out .10. but when we swich to module two we wanna disable all except .10. 
	- Could also use wired connection into the VLAN ports
- IDeal setup: GLinet connected to wan, lan into the 16 porter on .9.xx subnet, then do all 8 trunk ports from server boxes and switch edge nodes into the 16 port + 1 trunk transfer to the moker link is 9 trunk ports total. Then oen last admin port. 
- Eth3 connect to VLAN9 on 16 port.
- 14/16 ports used
- 5 untagged ports, muist assign a vlan to partition it but don't want it being seen by upstream
- **9,11,13,15 like he did port 5, reconfigure 5 to a trunk
# Other tasks 
- Fix cart wiring; color code ethernets and use short ones
- **Fill out new IPAM sheet/updated version; between 1:00pm-3:30pm
- Update cable management/packing plan with pictures of final setup first