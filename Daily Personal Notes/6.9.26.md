# To do
- ~~Hours - ask Patty and fill out timesheet - not needed bc REU
- ~~Ceph
- ~~Manual switches~~
-  ~~Doing vm cluster setup. 
- ~~All static IPs set for proxmox boxes
- ~~burn opensense ISO~~
- ~~opensens:
	- ~~Check the checksum, unzip it, burn to USB~~

# General Notes
- He will be out Friday and try to meet then if he can
-  all static ips for eht 0 and 3
- Trunk is like a tunnel. allows VLANs (the trunked ones) (eth 1) so each box can speak VLAN then do the same on each manual switch. can assign a vm to a VLAN then, and assign a different vlan to each switch port and physical things can talk to vms
- For each switch it uses a range of ten, like the twenties, then thirties, etc.
- Four VM firewalls in the ptgs.
- Port 5 will be management ip probably; sae subnet as vlan 2.
Vlan 2 should be vlan 10 probably for admin
Switch for them to talk on the admin channel needed too - managed switch
- ignore default vlans
- change admin one to 10 feom 2
- says not to use 0 or 1 for vlan but a higher one - ask network engineering team
- configure after spreadsheet ip mapping


for proxomox boxes need to nano hosts file in /etc for ips after chanign all ips
-

