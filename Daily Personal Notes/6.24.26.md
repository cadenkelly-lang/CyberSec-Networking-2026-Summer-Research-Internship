# Done
- Setting up firewall vm snapshots and configuration backups in case rules goes wrong
- Migrating old rules to new rules
- Trying to organize them appropriately
- Reading rules documentation to set the firewall rules up correctly

# Notes
- Order is sequential and matters; like a CS "or" statement
- Good rule of thumb is to have a rule at the bottom that denies any not specifically allowed traffic if system is built on only what it allows, or a rule at the bottom that allows any not specifically denied traffic if it is less restrictive


