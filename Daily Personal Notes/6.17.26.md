see if we can get the ntp by dhcp so we don't need to edit chrony each time. 
Look at docs and edit each opnsense one and check

# Done today
- DHCP finishing set up
- DNS set up
- chrony installed/all vms updated
- NTP on each client
- Making sure proper network device in the opnsense vm hardware pairings to the web gui settings (such as opt 1 is vnet2 so making sure that is VLAN30 and not 32)
- **Everything above exempts the Jump VM and DHCP VM for now**

# To do:
- Play with aliases
- Check all the above stuff
# Notes:
Module Plans:
It will probably be a mix of slides and demo, heavy on the demo part.
Will need to go over linux command line and core topics as I go through them. Use the OSI diagram and terminology thingy for them
learning how to find ip address, dns lookups, setting/changing ip, maybe grep/pipe cd, pwd, 
explain what command line is and how it works; also ctrl c to break things
ip address/subnet mask explanation after they can find it
DHCP, DNS, NTP, netowrk segmentation work
DHCP in module 1 is through a vm that has dnsmasq configured. DNS is done here too. NTP???
Can show me configuring it. Also, network segmentation by...??? Maybe 

module 1 has dhcp vm on and they get dynamic addresses while opnsense firewalls are on. can try pinging vm resources.

Many screenshots for what I did; recording/documenting our meetings helps me