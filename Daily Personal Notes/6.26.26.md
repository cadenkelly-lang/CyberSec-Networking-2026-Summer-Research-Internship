# Notes
- Firewall traffic doens't block own subnet because not leaving/crossing network
- can't ping on own subnet becasue router saying block it as last rule and getting to edge of netwrok
- can do sudo on clone after firrewall to check dns and internet
- Path: WAN> VLAN 20 gateway.20.1>VLAN20 client so when going up from client the NAT (network address translation) sees it as the WAN address
- masks all on WAN
- MASQ NAT many things behind one ip
- Provides no way back in for host outside wan to talk to individual ip

# Done
- ~~Figuring out and setting up firewall rules in opnsense on VLAN 20 as template, making network group, setting NAT. 
- ~~Diagram v4 with new hardware for both versions 1 and 2
	- version 1 is with the two 8 ports, 2 is with the 16 ports. Default to version 2
- ~~Finishing Edx courses required by company for sustainability certificate
- 16 port switch fully configured port-wise; the WAN ports acts like an unmanaged switch  that can connect to wifi's LAN


# To do
- aliases for proxmox nodes then allow vlans to talk to proxmox to install opnsense and do terminals
- maybe download wssh onto them
- Either use two 8 ports as the WAN swtich and trunk switch, or the 16 for both; either way, solves problems. Those are final configs 1 and 2
	- Replace GL-INET with opnsense. let it be the 10.xxx router. move it into opnsense. maybe.
- Backup USB for entire network config on proxmox. network file share = nfs