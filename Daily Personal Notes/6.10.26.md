# To-do
- ~~IPAM Spreadsheet (HP) - today do
	- *Needs to be continually updated but has stuff caught up to today's*
- ~~Trunk ports
- ~~VLAN configs partially, some VMs testing

Both HP should take a total of 2 hrs
Cable management and color coding may be after the final TG is set up as a fixer upper

# Morning Meeting Notes
- QEMU swhen you go to summary it lets (inside vm software)B That talks to proxmox. QEMU -guest-agent lets you see IP of system from proxmox
- linked clone doscn't recreate whole hard drive image and depdneant on og template usually do a full clone except for quick test
- must add users for a group inside terminal before in web interfae of proxmox - done in datacenter. they also need permission. pwd same as name
- create group oin roles
- can login to pve1 now with the student1 user who has only a few permissions with their own VM
- The pool is the group within each virtual enviornemnt that belongs to a switch that is trunked

# Afternoon Meeting Notes
- Using bridge 2 can put VMs wherwever
- Connecting nic2 to wan on network lets the boxes use WAN when considering the above bullet point
- 
